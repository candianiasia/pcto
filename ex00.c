#include <stdio.h>

int main()
{
    float x = 35.6; //flag : f%
    char g = 'a'; //flag : c%
    char h = 'b'; //flag : c%
    int y = 55; //float : c%

    printf("ho creato una variabile di tipo float %f,  il primo carattere è %c, il secomdo carattere è %c, il terzo carattere è %c\n", x, g, h, y);
}
